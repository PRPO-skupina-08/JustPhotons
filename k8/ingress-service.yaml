apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  class
spec:
  ingressClassName: nginx
  rules: 
  - http: 
      paths: 
        - path: /coordinator
          pathType: Prefix
          backend: 
            service: 
              name: coordinator
              port: 
                number: 8080
